spring:
  application:
    name: product-service
  profiles:
    active: localhost

---

spring:
  cloud:
    config:
      uri: http://localhost:8888
  config:
    activate:
      on-profile: localhost

---

spring:
  config:
    activate:
      on-profile: stage
  cloud:
    config:
      uri: http://config-server:8888