spring:
  application:
    name: config-server
  profiles:
    active: localhost

info:
  component: Config Server

---

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/pblgllgs/sb2-config-files-product-cache-product.git
  config:
    activate:
      on-profile: localhost
logging:
  level:
    root: debug
server:
  port: 8888

---

spring:
  config:
    activate:
      on-profile: stage
  cloud:
    config:
      server:
        git:
          uri: https://github.com/pblgllgs/sb2-config-files-product-cache-product.git
logging:
  level:
    root: debug
server:
  port: 8888