spring:
  application:
    name: eureka-server
  profiles:
    active: localhost
---

spring:
  config:
    activate:
      on-profile: localhost
  cloud:
    config:
      uri: http://localhost:8888

---

spring:
  config:
    activate:
      on-profile: stage
  cloud:
    config:
      uri: http://config-server:8888