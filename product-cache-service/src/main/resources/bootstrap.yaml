spring:
  application:
    name: eureka-server
  profiles:
    active: localhost
---
spring:
  cloud:
    config:
      uri: http://localhost:8888
  config:
    activate:
      on-profile: localhost
---
spring:
  cloud:
    config:
      uri: http://config-server:8888
  config:
    activate:
      on-profile: stage